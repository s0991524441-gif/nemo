/** Design reminder: "مدار سماوي" — keep the journey visible and let extraction, Excel, and CRM choices remain equally clear. */
import Home from "./pages/Home";

export default function App() {
  return <Home />;
}
