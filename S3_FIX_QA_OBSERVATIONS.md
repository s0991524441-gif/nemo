# S3-FIX QA OBSERVATIONS

| المسار أو الاختبار | النتيجة المرحلية |
|---|---|
| `JOB-1030 → Results` من التنقل العام | ظهرت حالة معالجة محجوبة، مع 67% وإجراءات الرجوع، من دون جدول أو `BUS-1375`. |
| `#/discovery/results?job=JOB-1030` | الرابط المباشر التقط `job` من Hash وطبق بوابة النتائج نفسها. |
| `JOB-1028 → Results` | عرض جدول النتائج المكتملة وعينة 3 Businesses مع ملخص 1,248 نتيجة. |
| `JOB-1027 failed → retry → completed` | بقيت النتائج محجوبة قبل الاكتمال ثم أصبحت متاحة بعد اكتمال المحاكاة. |
| `JOB-1030 → cancel → Results` | بقيت الحالة ملغاة والنتائج محجوبة، من دون جدول أو بيانات مستعارة. |
| Jobs filter = اليوم | ظهر كل من `JOB-1030` و`JOB-1028`. |
| معاينة الهاتف | نتائج Job المكتملة ظهرت صحيحة على 390×844. أظهرت بعض لقطات Jobs والـprocessing Sidebar أثناء انتقال الرسم في أداة المعاينة، بينما المسار المكتمل ظهر بلا Sidebar؛ سيُستكمل الفحص بعد استقرار العرض قبل الإغلاق. |
| الرابط المباشر للـJob processing | `#/discovery/results?job=JOB-1030` أصبح يلتقط Job من Hash ويعرض حالة محجوبة بلا جدول أو فلاتر أو بيانات Business. |
| الرابط المباشر للـJob failed/cancelled | الحالتان تعرضان State محجوبة وإجراءات صحيحة بلا نتائج مستعارة. |
| فلتر اليوم | ظهر `JOB-1030` و`JOB-1028` معًا عند اختيار «اليوم». |
